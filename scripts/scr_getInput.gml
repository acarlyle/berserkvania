///scr_getInput

m_leftArrow = keyboard_check(vk_left) || keyboard_check(ord('A'));
m_rightArrow = keyboard_check(vk_right) || keyboard_check(ord('D'));
m_spaceBar = keyboard_check(vk_space);
m_ctrl = keyboard_check(vk_control);
m_z = keyboard_check(ord('Z'));
